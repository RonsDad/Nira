{
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "outputDirectory": null,
  "installCommand": "npm install",
  "functions": {
    "api/chat.js": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    }
  ]
}
